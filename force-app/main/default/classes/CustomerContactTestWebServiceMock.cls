/**
 * This class contains unit tests for validating the behavior of Apex classes
 * and triggers.
 *
 * Unit tests are class methods that verify whether a particular piece
 * of code is working properly. Unit test methods take no arguments,
 * commit no data to the database, and are flagged with the testMethod
 * keyword in the method definition.
 *
 * All test methods in an organization are executed whenever Apex code is deployed
 * to a production organization to confirm correctness, ensure code
 * coverage, and prevent regressions. All Apex classes are
 * required to have at least 75% code coverage in order to be deployed
 * to a production organization. In addition, all triggers must have some code coverage.
 * 
 * The @isTest class annotation indicates this class only contains test
 * methods. Classes defined with the @isTest annotation do not count against
 * the organization size limit for all Apex scripts.
 *
 * See the Apex Language Reference for more information about Testing and Code Coverage.
 */
@isTest
global class CustomerContactTestWebServiceMock implements WebServiceMock {
   global void doInvoke(
           Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType) {
		 sapComDocumentSapSoapFunctionsMcShana.Zcrmsdtt00038 Reply=new sapComDocumentSapSoapFunctionsMcShana.Zcrmsdtt00038();
        list<sapComDocumentSapSoapFunctionsMcShana.Zcrmsdst00038> listitem=new list<sapComDocumentSapSoapFunctionsMcShana.Zcrmsdst00038>();
       		sapComDocumentSapSoapFunctionsMcShana.Zcrmsdst00038 item1=new sapComDocumentSapSoapFunctionsMcShana.Zcrmsdst00038();
       		item1.ISERROR='';
       		item1.CustomerID='0998222';
       		listitem.add(item1);
       		Reply.item=listitem;
       		sapComDocumentSapSoapFunctionsMcShana.ZsfCustomerMasterResponse_element RespElement=new sapComDocumentSapSoapFunctionsMcShana.ZsfCustomerMasterResponse_element();
       		list<sapComDocumentSapSoapFunctionsMcShana.Zexdsdst00097> msg=new list<sapComDocumentSapSoapFunctionsMCShana.Zexdsdst00097>();
  			sapComDocumentSapSoapFunctionsMcShana.Zexdsdtt00052 rslt=new sapComDocumentSapSoapFunctionsMcShana.Zexdsdtt00052();
  			sapComDocumentSapSoapFunctionsMCShana.Zexdsdst00097 Message=new sapComDocumentSapSoapFunctionsMCShana.Zexdsdst00097();
  			Message.Message='hahaha';
  			Message.MessageNo='1';
  			msg.add(Message);
  			rslt.item=msg;
  			item1.Messages=rslt;
       		respElement.EtReturn=Reply;
       		response.put('response_x',RespElement);
        // Create response element from the autogenerated class.

        // Populate response element.

        // Add response element to the response parameter, as follows:


   }

}